//imports - router, and any model necessary
const router = require("express").Router();
const { User } = require("../../models");

//Endpoint in this file: "/api/posts"... 

//get route - do we need one for Post?

//post route - for new posts to a character page...or should this just be in charater routes as a post route???
// router.post("/")



//export router
module.exports = router;